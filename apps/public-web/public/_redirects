# Application monopage : toute URL inconnue est servie par index.html, pour
# que /terrains/:id ou /espace-client fonctionnent en accès direct et au
# rafraîchissement. Le code 200 sert la page sans redirection visible.
/*    /index.html   200
